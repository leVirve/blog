language: node_js

node_js:
  - "6"

cache:
  directories:
    - node_modules

script:
  - npm run build

after_success:
  - cd ./public
  - git init
  - git config user.name "leVirve@Travis" 
  - git config user.email "gae.m.project@gmail.com"
  - git add --all .
  - git commit -m "Travis CI Auto Builder"
  - git push --quiet --force https://$DEPLOY_TOKEN@github.com/leVirve/leVirve.github.io.git master

branches:
  only:
    - raw
