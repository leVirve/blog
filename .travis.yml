language: node_js

node_js:
  - "6"

cache:
  directories:
    - node_modules

script:
  - npm run build

after_success:
  - git config --global user.name "leVirve@Travis" 
  - git config --global user.email "gae.m.project@gmail.com"
  - npm run deploy

branches:
  only:
    - raw
